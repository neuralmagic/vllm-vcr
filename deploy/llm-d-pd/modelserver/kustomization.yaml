apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - prefill-deployment.yaml
  - decode-deployment.yaml

# Repoint the image here (e.g. to a freshly built tag) without editing the Deployments.
images:
  - name: ghcr.io/neuralmagic/inference-simulator-rs
    newTag: vllm0.23
