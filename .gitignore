/target
/target-linux
# Captured trace data (large, and token-recording traces carry model content).
# The inventory README stays tracked.
/traces/*
!/traces/README.md

# mdBook render output (the doc site is built in CI and deployed to Pages).
/book
